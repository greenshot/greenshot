<UserControl x:Class="Greenshot.Plugin.RecipeEditor.Views.RecipeEditorConfigurationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:wpf="clr-namespace:Greenshot.Base.Wpf;assembly=Greenshot.Base">
    <UserControl.Resources>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="{Binding Source={x:Static wpf:ThemeManager.Instance}, Path=ForegroundBrush}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{Binding Source={x:Static wpf:ThemeManager.Instance}, Path=ForegroundBrush}"/>
            <Setter Property="Margin" Value="0,6"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Foreground" Value="{Binding Source={x:Static wpf:ThemeManager.Instance}, Path=ForegroundBrush}"/>
            <Setter Property="Background" Value="{Binding Source={x:Static wpf:ThemeManager.Instance}, Path=BackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{Binding Source={x:Static wpf:ThemeManager.Instance}, Path=ControlBorderBrush}"/>
            <Setter Property="Margin" Value="0,4"/>
            <Setter Property="Padding" Value="4,2"/>
        </Style>
    </UserControl.Resources>

    <StackPanel Margin="8">
        <CheckBox IsChecked="{Binding Enabled}"
                  Content="{wpf:Translate expertsettings_enablerecipes}"/>

        <CheckBox IsChecked="{Binding QuicklinkEnabled}"
                  Content="{wpf:Translate quicklink_enable}"/>

        <Label Content="Configured Recipe Files (semicolon-separated):" Margin="0,8,0,2"/>
        <TextBox Text="{Binding RecipeFiles, UpdateSourceTrigger=PropertyChanged}"/>
    </StackPanel>
</UserControl>

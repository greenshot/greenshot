<UserControl x:Class="Greenshot.Plugin.Zxing.Forms.ZxingConfigurationControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:wpf="clr-namespace:Greenshot.Base.Wpf;assembly=Greenshot.Base">
    <UserControl.Resources>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{Binding Source={x:Static wpf:ThemeManager.Instance}, Path=ForegroundBrush}"/>
            <Setter Property="Margin" Value="0,8"/>
        </Style>
    </UserControl.Resources>

    <StackPanel Margin="8">
        <CheckBox IsChecked="{Binding ScanOnCapture}"
                  Content="{wpf:Translate zxing_scan_on_capture}"/>
    </StackPanel>
</UserControl>

<Window x:Class="Greenshot.Forms.ModernBugReportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:Greenshot.ViewModels"
        mc:Ignorable="d"
        Title="Greenshot - Unexpected Error" Width="680"
        SizeToContent="Height" MaxHeight="850"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource WindowBackground}"
        Foreground="{DynamicResource PrimaryText}"
        FontFamily="Segoe UI"
        WindowStyle="None"
        BorderThickness="1"
        BorderBrush="{DynamicResource BorderBrush}">
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="32" 
                      ResizeBorderThickness="5" 
                      GlassFrameThickness="0" 
                      CornerRadius="0"/>
    </WindowChrome.WindowChrome>
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        
        <Style x:Key="TitleBarButtonStyle" TargetType="Button">
            <Setter Property="WindowChrome.IsHitTestVisibleInChrome" Value="True"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Background" Value="{DynamicResource ButtonBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource ButtonForeground}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.8"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Custom Title Bar -->
        <Grid Grid.Row="0" Background="{DynamicResource TitleBarBackground}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="10,0" VerticalAlignment="Center">
                <Image Source="{Binding Icon, RelativeSource={RelativeSource AncestorType=Window}}" 
                       Width="16" Height="16" 
                       VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBlock Text="Greenshot Error" VerticalAlignment="Center" FontSize="12" Foreground="{DynamicResource PrimaryText}"/>
            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <Button Click="Minimize_Click">
                    <TextBlock Text="─" FontSize="10" VerticalAlignment="Center"/>
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource TitleBarButtonStyle}">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource TitleBarButtonHover}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                <Button Click="Close_Click">
                    <TextBlock Text="✕" FontSize="10" VerticalAlignment="Center"/>
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource TitleBarButtonStyle}">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource TitleBarCloseHover}"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Content Area -->
        <Grid Grid.Row="1" Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Greenshot has encountered an unexpected error" FontSize="22" FontWeight="SemiBold" Foreground="#D32F2F" Margin="0,0,0,15"/>
            
            <Border Grid.Row="1" Background="{DynamicResource WarningBackground}" BorderBrush="{DynamicResource WarningBorder}" BorderThickness="1" Padding="12" Margin="0,0,0,15" 
                    Visibility="{Binding IsUpdateAvailable, Converter={StaticResource BooleanToVisibilityConverter}}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="⚠" FontSize="18" Margin="0,0,10,0" VerticalAlignment="Center" Foreground="{DynamicResource WarningForeground}"/>
                    <TextBlock TextWrapping="Wrap" VerticalAlignment="Center" Foreground="{DynamicResource WarningForeground}">
                        <Run Text="A newer version of Greenshot is available (" FontWeight="SemiBold"/>
                        <Run Text="{Binding LatestVersion}" FontWeight="Bold"/>
                        <Run Text="). Please update, as this issue might already be fixed." FontWeight="SemiBold"/>
                    </TextBlock>
                </StackPanel>
            </Border>

            <StackPanel Grid.Row="2" Margin="0,0,0,15">
                <TextBlock Text="Exception Details" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,8" Foreground="{DynamicResource PrimaryText}"/>
                <Border Background="#FEEBEE" Padding="10" CornerRadius="4" Margin="0,0,0,15" BorderBrush="#FFCDD2" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="{Binding ExceptionType}" FontWeight="Bold" Foreground="#C62828" Margin="0,0,0,4"/>
                        <TextBlock Text="{Binding Message}" TextWrapping="Wrap" Foreground="#424242"/>
                    </StackPanel>
                </Border>

                <TextBlock Text="Stability Hash" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,5" Foreground="{DynamicResource PrimaryText}"/>
                <TextBlock Text="This hash helps us identify if this bug has been reported before:" FontSize="11" Margin="0,0,0,5" Foreground="{DynamicResource SecondaryText}"/>
                <Border Background="{DynamicResource CardBackground}" Padding="8" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="4" Margin="0,0,0,10">
                    <TextBlock Text="{Binding Hash}" FontFamily="Consolas" Foreground="{DynamicResource PrimaryText}" FontWeight="SemiBold"/>
                </Border>
            </StackPanel>

            <Expander Grid.Row="3" Header="Full Diagnostic Report" Foreground="{DynamicResource PrimaryText}" Margin="0,0,0,10">
                <TextBox Text="{Binding BugReport, Mode=OneWay}" 
                         IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap" 
                         Height="350" VerticalScrollBarVisibility="Auto"
                         FontFamily="Consolas" FontSize="11" Margin="0,10,0,0" 
                         BorderThickness="1" BorderBrush="{DynamicResource BorderBrush}" 
                         Background="{DynamicResource InputBackground}" 
                         Foreground="{DynamicResource PrimaryText}"/>
            </Expander>

            <Separator Grid.Row="4" Margin="0,10,0,20" Background="{DynamicResource BorderBrush}"/>

            <Grid Grid.Row="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" VerticalAlignment="Center" Foreground="{DynamicResource SecondaryText}" FontSize="11" TextWrapping="Wrap" Margin="0,0,10,0">
                    <Run Text="Searching GitHub will use the stability hash to find similar issues."/>
                </TextBlock>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="Search GitHub" Click="SearchGitHub_Click" Background="#24292E" Foreground="White" BorderThickness="0" FontWeight="SemiBold"/>
                    <Button Content="Copy Report" Click="CopyToClipboard_Click"/>
                    <Button Content="Close" IsCancel="True" Click="Close_Click"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</Window>
